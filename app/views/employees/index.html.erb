<p style="color: green"><%= notice %></p>

<h1>Employees</h1>

<div id="employees">
  <% @employees.each do |employee| %>
    <%= render employee %>
    <p>
      <%= link_to "Show this employee", employee %> || 
      <%= link_to "Edit this employee", edit_employee_path(employee) %>
    </p>
  <% end %>
</div>

<%= @pagy.next %>

<%= pagy_url_for(@pagy, @pagy.next) %>

<%= turbo_frame_tag "employees-page-#{@pagy.next}", loading: :lazy, src: pagy_url_for(@pagy, @pagy.next)  %>

<%#= link_to "New employee", new_employee_path %>
